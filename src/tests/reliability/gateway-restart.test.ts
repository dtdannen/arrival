import { describe, it } from 'vitest'

describe('Reliability — Gateway Restart', () => {
  it.todo('T-1900: Gateway restart preserves nullifier integrity — replay rejected with duplicate_nullifier')
})

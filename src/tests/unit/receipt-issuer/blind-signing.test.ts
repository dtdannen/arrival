import { describe, it } from 'vitest'

describe('Receipt Issuer — Blind Signing', () => {
  it.todo('T-400: Blind signing happy path — returns S_blind, unblinding produces valid RSA signature')

  it.todo('T-401: End-to-end receipt flow — client blinds r, issuer signs, client unblinds, Verify(pk, r, S) succeeds')
})

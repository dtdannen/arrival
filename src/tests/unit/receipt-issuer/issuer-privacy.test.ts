import { describe, it } from 'vitest'

describe('Receipt Issuer — Privacy', () => {
  it.todo('T-406: Issuer does not learn r — only blinded value B visible to issuer')

  it.todo('T-407: Issuer cannot link S_blind to unblinded (r, S) — no deterministic linkage across batch')
})

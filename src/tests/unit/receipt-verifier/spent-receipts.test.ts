import { describe, it } from 'vitest'

describe('Receipt Verifier — Spent Receipts', () => {
  it.todo('T-504: Spent receipt rejected — duplicate receipt_hash returns invalid_interaction_proof')

  it.todo('T-505: Keyset period outside claimed time window rejected — invalid_timeblind_proof')
})

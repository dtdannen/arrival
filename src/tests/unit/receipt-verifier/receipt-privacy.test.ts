import { describe, it } from 'vitest'

describe('Receipt Verifier — Privacy', () => {
  it.todo('T-506: Receipt does not contain reviewer identity — no persistent identity, Nostr keys, or WoT position')
})

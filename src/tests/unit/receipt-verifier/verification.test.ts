import { describe, it } from 'vitest'

describe('Receipt Verifier — Verification', () => {
  it.todo('T-500: Valid receipt accepted — passes all checks')

  it.todo('T-501: Unknown keyset_id rejected — invalid_interaction_proof')

  it.todo('T-502: Wrong subject_id for keyset rejected — invalid_interaction_proof')

  it.todo('T-503: Invalid RSA signature rejected — invalid_interaction_proof')
})

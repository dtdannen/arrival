import { describe, it } from 'vitest'

describe('Review Feed API — Verification Endpoint', () => {
  it.todo('T-1230: Returns proof metadata and verification status')

  it.todo('T-1231: No exact timestamps in verification response — time_window_id only')
})

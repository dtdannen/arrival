import { describe, it } from 'vitest'

describe('Review Feed API — Feed Queries', () => {
  it.todo('T-1220: Returns only published reviews')

  it.todo('T-1221: Filter by max_distance — only matching distance reviews returned')

  it.todo('T-1222: Filter by verified_only — only fully verified reviews returned')

  it.todo('T-1223: No created_at in response — only time_window_id as timing signal')

  it.todo('T-1224: Per-review fields match spec — review_id, subject_id, content, time_window_id, distance_bucket, verification_badges')
})

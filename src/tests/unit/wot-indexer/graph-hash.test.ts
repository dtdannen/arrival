import { describe, it } from 'vitest'

describe('WoT Indexer — Graph Snapshot Hash', () => {
  it.todo('T-207: Deterministic graph_snapshot_hash — identical hash regardless of ingestion order')

  it.todo('T-208: graph_snapshot_hash changes when graph changes — new edge produces different hash')
})

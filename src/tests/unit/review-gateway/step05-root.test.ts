import { describe, it } from 'vitest'

describe('Review Gateway — Step 5: Root Verification', () => {
  it.todo('T-940: Active root passes')

  it.todo('T-941: Unknown root rejected — inactive_root')

  it.todo('T-942: Expired root rejected — inactive_root')
})

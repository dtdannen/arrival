import { describe, it } from 'vitest'

describe('Review Gateway — Step 3: Proof Version', () => {
  it.todo('T-920: Supported proof version passes')

  it.todo('T-921: Unsupported proof version rejected — unsupported_proof_version')

  it.todo('T-922: proof_version checked before bundle unpacking — rejects with unsupported_proof_version not invalid_schema')
})

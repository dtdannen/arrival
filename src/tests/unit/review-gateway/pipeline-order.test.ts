import { describe, it } from 'vitest'

describe('Review Gateway — Pipeline Order', () => {
  it.todo('T-995: Cheapest checks run first — signature failure before membership failure')

  it.todo('T-996: Each step has exactly one reject code — 10 steps, 10 codes, 1:1 mapping')

  it.todo('T-997: Rejected submission does not consume nullifier — corrected resubmit accepted')
})

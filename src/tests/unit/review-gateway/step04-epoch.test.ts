import { describe, it } from 'vitest'

describe('Review Gateway — Step 4: Epoch Context', () => {
  it.todo('T-930: Matching epoch passes')

  it.todo('T-931: Mismatched epoch rejected — invalid_epoch_context')

  it.todo('T-932: Server derives epoch authoritatively — gateway computes epoch_id from policy context')
})

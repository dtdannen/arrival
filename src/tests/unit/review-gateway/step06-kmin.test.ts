import { describe, it } from 'vitest'

describe('Review Gateway — Step 6: k_min Threshold', () => {
  it.todo('T-950: Cohort above k_min passes')

  it.todo('T-951: Cohort below k_min hard-rejected — insufficient_anonymity_set')

  it.todo('T-952: k_size read from server, not client — server-side k_size used')

  it.todo('T-953: Below-k_min never admitted or held — reject, not deferred')
})

import { describe, it } from 'vitest'

describe('Review Gateway — Step 1: Signature Verification', () => {
  it.todo('T-900: Valid Ed25519 signature passes')

  it.todo('T-901: Invalid signature rejected — invalid_signature')

  it.todo('T-902: Signature over wrong payload rejected — invalid_signature')
})

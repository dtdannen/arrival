import { describe, it } from 'vitest'

describe('Review Gateway — Step 10: Nullifier Uniqueness', () => {
  it.todo('T-990: Unique nullifier passes — nullifier stored')

  it.todo('T-991: Duplicate nullifier rejected — duplicate_nullifier')

  it.todo('T-992: Same nullifier in different epoch passes — scoped to (subject_id, epoch_id)')
})

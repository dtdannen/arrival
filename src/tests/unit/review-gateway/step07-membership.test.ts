import { describe, it } from 'vitest'

describe('Review Gateway — Step 7: Membership Proof', () => {
  it.todo('T-960: Valid ZK membership proof passes')

  it.todo('T-961: Invalid membership proof rejected — invalid_membership_proof')
})

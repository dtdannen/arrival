import { describe, it } from 'vitest'

describe('Identity Client — Submission Privacy', () => {
  it.todo('T-102: Persistent key never appears in submission — Nostr pubkey absent from all fields of review_submission_v1')
})

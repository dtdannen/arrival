import { describe, it } from 'vitest'

describe('Identity Client — Derive Posting Key', () => {
  it.todo('T-100: Derive one-time posting key from persistent identity — key is valid Ed25519, distinct from persistent key, deterministic')

  it.todo('T-101: Posting key signs review payload — valid Ed25519 signature that verifies against the posting key')
})

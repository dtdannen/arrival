import { describe, it } from 'vitest'

describe('Cohort Root Publisher — Merkle Tree', () => {
  it.todo('T-300: Build Merkle tree per distance tier — three roots with correct members and k_size')

  it.todo('T-301: Root metadata includes required fields — root_hash, k_size, distance_bucket, graph_snapshot_hash, valid_from, valid_to')
})

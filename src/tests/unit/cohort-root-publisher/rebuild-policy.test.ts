import { describe, it } from 'vitest'

describe('Cohort Root Publisher — Rebuild Policy', () => {
  it.todo('T-302: Root not rebuilt when graph unchanged — existing root reused')

  it.todo('T-303: Root rebuilt when graph changes — new root with updated graph_snapshot_hash')

  it.todo('T-304: Expired root is not active — expired root not returned from active roots query')
})

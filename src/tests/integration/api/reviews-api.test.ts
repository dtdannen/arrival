import { describe, it } from 'vitest'

describe('Integration — Reviews API', () => {
  it.todo('T-1220: GET /v1/subjects/{subject_id}/reviews returns only published reviews')

  it.todo('T-1221: Filter by max_distance works correctly')

  it.todo('T-1222: Filter by verified_only works correctly')
})

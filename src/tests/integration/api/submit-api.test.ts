import { describe, it } from 'vitest'

describe('Integration — Submit API', () => {
  it.todo('T-1210: Valid submission returns admitted with held_reason and verified_flags')

  it.todo('T-1211: Rejected submission returns reject_code and reject_detail')

  it.todo('T-1212: verified_flags reflect individual checks — all flags present and correct')
})

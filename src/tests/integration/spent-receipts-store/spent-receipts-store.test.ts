import { describe, it } from 'vitest'

describe('Integration — Spent Receipts Store', () => {
  it.todo('T-1600: receipt_hash stored on admission — Hash(r) with subject_id and spent_at')

  it.todo('T-1601: Spent receipt blocks reuse in any epoch — epoch-independent rejection')

  it.todo('T-1602: Different receipts with same subject accepted — different receipt_hash values')
})

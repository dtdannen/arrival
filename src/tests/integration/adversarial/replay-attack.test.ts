import { describe, it } from 'vitest'

describe('Adversarial — Replay Attack', () => {
  it.todo('T-1400: Same payload resubmitted — rejected with duplicate_nullifier')

  it.todo('T-1407: Concurrent duplicate nullifier race condition — exactly one admitted, one rejected')
})

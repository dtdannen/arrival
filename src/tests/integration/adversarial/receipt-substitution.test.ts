import { describe, it } from 'vitest'

describe('Adversarial — Receipt Substitution', () => {
  it.todo('T-1402: Wrong receipt for subject — rejected with invalid_interaction_proof')

  it.todo('T-1408: Issuer collusion receipt for non-customer — passes verification (known residual risk)')
})

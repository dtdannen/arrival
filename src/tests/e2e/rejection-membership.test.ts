import { describe, it } from 'vitest'

describe('E2E — Rejection: Invalid Membership', () => {
  it.todo('T-2101: Identity not in cohort — rejected with invalid_membership_proof, nullifier not consumed')
})

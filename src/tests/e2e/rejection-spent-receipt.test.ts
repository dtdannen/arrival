import { describe, it } from 'vitest'

describe('E2E — Rejection: Spent Receipt', () => {
  it.todo('T-2102: Two submissions with same receipt — first admitted, second rejected with invalid_interaction_proof')
})

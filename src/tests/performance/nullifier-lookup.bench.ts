import { describe, it } from 'vitest'

// Will be converted to bench() calls when implemented.
// Run with: npm run test:perf (vitest bench)

describe('Performance — Nullifier Lookup', () => {
  it.todo('T-1803: Nullifier existence query with 100k+ entries — latency within target')
})

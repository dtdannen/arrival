import { describe, it } from 'vitest'

// Will be converted to bench() calls when implemented.
// Run with: npm run test:perf (vitest bench)

describe('Performance — Browser Proving', () => {
  it.todo('T-1804: Full proof bundle generation via snarkjs WASM — acceptable time on target hardware')
})

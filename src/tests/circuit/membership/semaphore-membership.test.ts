import { describe, it } from 'vitest'

describe('Circuit — Semaphore v4 Membership', () => {
  it.todo('T-700: Valid membership proof against active root — proof valid')

  it.todo('T-701: Membership proof against wrong root fails — proof invalid')

  it.todo('T-702: Non-member cannot produce valid proof')
})

{
  "name": "arrival-mvp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:unit": "vitest run --project unit",
    "test:circuit": "vitest run --project circuit",
    "test:integration": "vitest run --project integration",
    "test:e2e": "vitest run --project e2e",
    "test:perf": "vitest bench",
    "test:reliability": "vitest run --project reliability",
    "test:watch": "vitest --project unit",
    "test:watch:all": "vitest"
  },
  "devDependencies": {
    "@noble/curves": "^1.9.7",
    "@noble/ed25519": "^2.3.0",
    "@noble/hashes": "^1.8.0",
    "@semaphore-protocol/core": "^4.14.2",
    "@semaphore-protocol/group": "^4.14.2",
    "@semaphore-protocol/identity": "^4.14.2",
    "@semaphore-protocol/proof": "^4.14.2",
    "@vitest/coverage-v8": "^3.0.0",
    "circomlibjs": "^0.1.7",
    "nostr-tools": "^2.23.3",
    "snarkjs": "^0.7.6",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
